#include <stdio.h>

int main(void) {
        int counter = 1;
        while (counter <= 5) {
                printf("%d\n", counter);
                ++counter;
        }
        // More code here...
}
